export interface ISideBarList{
    id: number;
    name: string;
    route?: string;
}

export interface IDashboardOverview {
    id: number;
    name: string;
    number: number;
}

export interface IHeadList {
    id: number;
    name: string;
}